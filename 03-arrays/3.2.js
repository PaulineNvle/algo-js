let  tableau = [1, 2, 3, 4, 5];
let  add = 0;

for (let i = 0; i < tableau.length; i++) {
  add += tableau[i];
}   console.log(add);

 // faire d'abord une somme (comme 3.1) puis diviser la somme par la longueur du tableau (compte les éléments dedans)
let average= add / tableau.length;
  console.log(average);

  