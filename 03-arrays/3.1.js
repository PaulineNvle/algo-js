
//
// let  tableau = [1, 2, 3, 4, 5];
// let  add = 0;

// for (let i = 0; i < tableau.length; i++) {
//   add += tableau[i];
// }   console.log(add);



// faire une somme des element avec un for = on commence a 0 pour l'itération, on ajoute les elements du tableau avec tableau lenght
// puis on fait + 1 mais axé sur le tableau lenght, pas réélement +1
// 0 + 100 puis on avance de 1 dans le tableau, donc 100 + 101, on avance encore 202 + 102


let  tableau = [100, 101, 102];
let  add = 0;

for (let i = 0; i < tableau.length; i++) {
  add += tableau[i];
}   console.log(add);
